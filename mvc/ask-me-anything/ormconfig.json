{
  "type": "sqlite",
  "database": "./db.sqlite",
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "synchronize": true,
  "logging": true,
  "migrations": ["dist/migration/*{.ts,.js}"],
  "cli": {
    "migrationsDir": "migration"
  }
}
